# Serving only: the FastAPI inference API and its Docker image.
# For the data pipeline, training, and tests, use requirements-train.txt.

torch==2.6.0
transformers==5.9.0
peft==0.19.1
bitsandbytes==0.49.2
fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
